# Agent 工作流模式說明文件

本專案展示了多種 Agent 工作流模式，每個模式都有對應的實用範例。

## 📋 工作流模式總覽

| 模式 | Agent | 說明 |
|------|-------|------|
| **A2A (Agent-to-Agent)** | `it_helpdesk` | 協調者模式，主 Agent 派發給子 Agent |
| **Sequential** | `expense` | 順序執行，每步驟的輸出是下一步的輸入 |
| **Parallel** | `research` | 平行執行多個任務，最後彙整結果 |
| **Loop** | `ticket` | 迴圈處理直到滿足終止條件 |
| **Human-in-the-Loop** | `order` | 自動化 + 人工審核的混合模式 |

---

## 🤝 A2A (Agent-to-Agent) - IT 服務台

### 概念
協調者模式：一個主要 Agent 負責理解需求，然後將任務派發給適當的專業子 Agent。

### 架構圖
```
                    ┌─────────────────┐
                    │   用戶問題      │
                    └────────┬────────┘
                             │
                             ▼
                    ┌─────────────────┐
                    │  IT 服務台總機   │
                    │  (Coordinator)  │
                    └────────┬────────┘
                             │
        ┌────────────────────┼────────────────────┐
        │                    │                    │
        ▼                    ▼                    ▼
┌───────────────┐   ┌───────────────┐   ┌───────────────┐
│   網路問題     │   │   帳號權限     │   │   硬體報修     │
│   Sub-Agent   │   │   Sub-Agent   │   │   Sub-Agent   │
└───────────────┘   └───────────────┘   └───────────────┘
```

### API 呼叫範例
```bash
# 建立 IT 工單
curl -X POST http://localhost:8000/agents/it_helpdesk/chat \
  -H "Content-Type: application/json" \
  -d '{"message": "我的網路一直斷線，而且 VPN 也連不上"}'

# 查詢工單狀態
curl -X POST http://localhost:8000/agents/it_helpdesk/chat \
  -d '{"message": "查詢工單 IT-20251216-001 的處理進度"}'
```

### 適用場景
- 客服系統分流
- IT 服務台
- 多部門協作

---

## ➡️ Sequential Workflow - 報銷審批

### 概念
嚴格的順序執行，每個步驟必須完成後才能進入下一步，且每步的輸出會影響後續流程。

### 架構圖
```
┌─────────┐   ┌─────────┐   ┌─────────┐   ┌─────────┐   ┌─────────┐
│  提交   │ → │  主管   │ → │  總監   │ → │  財務   │ → │  付款   │
│  報銷   │   │  審核   │   │  審核   │   │  處理   │   │  完成   │
└─────────┘   └─────────┘   └─────────┘   └─────────┘   └─────────┘
                  │              │
                  ▼              ▼
             (金額≤5000      (金額>5000
              可跳過)         必須審核)
```

### 條件分支邏輯
- 金額 ≤ $5,000：主管審核即可
- 金額 > $5,000 且 ≤ $20,000：需總監審核
- 金額 > $20,000：需更高層審核

### API 呼叫範例
```bash
# 提交報銷單
curl -X POST http://localhost:8000/agents/expense/chat \
  -d '{"message": "我要提交一筆報銷，交通費 3500 元，商務拜訪客戶"}'

# 主管審核
curl -X POST http://localhost:8000/agents/expense/chat \
  -d '{"message": "我是張主管，核准報銷單 EXP-2025-001"}'

# 查詢狀態
curl -X POST http://localhost:8000/agents/expense/chat \
  -d '{"message": "查詢報銷單 EXP-2025-001 的審批進度"}'
```

### 適用場景
- 費用審批流程
- 採購流程
- 請假審批
- 文件簽核

---

## ⚡ Parallel Workflow - 研究助理

### 概念
同時執行多個獨立任務，最後將結果彙整成一份報告。這可以大幅減少總執行時間。

### 架構圖
```
                    ┌─────────────┐
                    │  研究主題    │
                    └──────┬──────┘
                           │
        ┌──────────────────┼──────────────────┐
        ▼                  ▼                  ▼
┌───────────────┐ ┌───────────────┐ ┌───────────────┐
│   搜尋新聞    │ │  搜尋論文    │ │  搜尋數據    │
│   (並行)     │ │   (並行)     │ │   (並行)     │
└───────┬───────┘ └───────┬───────┘ └───────┬───────┘
        │                  │                  │
        └──────────────────┼──────────────────┘
                           ▼
                    ┌─────────────┐
                    │  彙整報告   │
                    └─────────────┘
```

### API 呼叫範例
```bash
# 研究特定主題
curl -X POST http://localhost:8000/agents/research/chat \
  -d '{"message": "幫我研究 AI 人工智慧的市場趨勢"}'

# 搜尋論文
curl -X POST http://localhost:8000/agents/research/chat \
  -d '{"message": "搜尋關於半導體製程的最新論文"}'

# 取得市場數據
curl -X POST http://localhost:8000/agents/research/chat \
  -d '{"message": "電動車市場的統計數據是什麼？"}'
```

### 適用場景
- 市場研究
- 競品分析
- 資料彙整
- 多來源驗證

---

## 🔄 Loop Workflow - 客服工單

### 概念
持續迴圈處理直到滿足終止條件。工單會在不同狀態間流轉，直到解決或關閉。

### 架構圖
```
┌─────────────────────────────────────────────────────────┐
│                                                         │
│    ┌──────────┐     ┌──────────┐     ┌──────────┐     │
│    │  建立    │────▶│  處理中  │────▶│  待回覆  │     │
│    │  工單    │     │          │     │          │     │
│    └──────────┘     └────┬─────┘     └────┬─────┘     │
│                          │                 │           │
│                          ▼                 ▼           │
│                    ┌──────────┐     ┌──────────┐     │
│                    │ 等待資訊 │◀───│ 客戶回覆 │     │
│                    │          │     │          │     │
│                    └────┬─────┘     └──────────┘     │
│                          │                           │
│          ┌───────────────┴───────────────┐           │
│          │                               │           │
│          ▼                               ▼           │
│    ┌──────────┐                   ┌──────────┐     │
│    │  已解決  │                   │  已關閉  │     │
│    └──────────┘                   └──────────┘     │
│                                                     │
└─────────────────────────────────────────────────────┘
```

### 狀態說明
- `open` - 新建立
- `in_progress` - 處理中
- `waiting_customer` - 等待客戶回覆
- `waiting_info` - 等待內部資訊
- `resolved` - 已解決
- `closed` - 已關閉

### API 呼叫範例
```bash
# 建立工單
curl -X POST http://localhost:8000/agents/ticket/chat \
  -d '{"message": "建立工單：客戶王小明無法登入系統，緊急程度高"}'

# 更新工單狀態
curl -X POST http://localhost:8000/agents/ticket/chat \
  -d '{"message": "將工單 TK-20251216-001 更新為處理中，備註：已開始排查"}'

# 新增客戶回覆
curl -X POST http://localhost:8000/agents/ticket/chat \
  -d '{"message": "工單 TK-20251216-001 收到客戶回覆：問題已解決，感謝"}'

# 關閉工單
curl -X POST http://localhost:8000/agents/ticket/chat \
  -d '{"message": "將工單 TK-20251216-001 標記為已解決"}'
```

### 適用場景
- 客服工單系統
- Bug 追蹤
- 問題處理流程
- 任務管理

---

## 👥 Human-in-the-Loop - 訂單處理

### 概念
結合自動化處理和人工審核。系統自動執行大部分流程，但在特定節點需要人工介入做決策。

### 架構圖
```
┌─────────────┐
│  接收訂單   │
└──────┬──────┘
       │
       ▼
┌─────────────┐
│  自動驗證   │──────────▶ ❌ 驗證失敗 ──▶ 🧑 人工審核
└──────┬──────┘
       │ ✓ 驗證通過
       ▼
┌─────────────┐
│  庫存檢查   │──────────▶ ⚠️ 庫存不足 ──▶ 🧑 人工決策
└──────┬──────┘                              (部分出貨/等待/取消)
       │ ✓ 庫存充足
       ▼
┌─────────────┐
│  風控檢查   │──────────▶ 🚨 高風險 ──▶ 🧑 人工審核
└──────┬──────┘
       │ ✓ 風控通過
       ▼
┌─────────────┐
│  自動出貨   │
└─────────────┘
```

### 人工介入點
1. **驗證失敗**：地址不完整、金額異常
2. **庫存不足**：決定部分出貨、等待補貨或取消
3. **風控警示**：新客戶大額訂單、超過信用額度、高風險客戶

### API 呼叫範例
```bash
# 建立訂單
curl -X POST http://localhost:8000/agents/order/chat \
  -d '{"message": "建立訂單：客戶王小明，購買 iPhone 15 Pro 一台，配送到台北市信義區，信用卡付款"}'

# 執行驗證（自動）
curl -X POST http://localhost:8000/agents/order/chat \
  -d '{"message": "驗證訂單 ORD-2025-002"}'

# 檢查庫存（自動/可能需人工）
curl -X POST http://localhost:8000/agents/order/chat \
  -d '{"message": "檢查訂單 ORD-2025-002 的庫存"}'

# 人工核准（如果需要）
curl -X POST http://localhost:8000/agents/order/chat \
  -d '{"message": "我是審核員張三，核准訂單 ORD-2025-002，備註：已確認客戶身份"}'

# 處理庫存不足決策
curl -X POST http://localhost:8000/agents/order/chat \
  -d '{"message": "訂單 ORD-2025-002 庫存不足，決定部分出貨，審核員李四"}'

# 處理出貨
curl -X POST http://localhost:8000/agents/order/chat \
  -d '{"message": "處理訂單 ORD-2025-002 出貨"}'

# 查看審核隊列
curl -X POST http://localhost:8000/agents/order/chat \
  -d '{"message": "查看目前有哪些訂單等待審核"}'
```

### 適用場景
- 電商訂單處理
- 貸款審批
- 內容審核
- 品質檢驗

---

## 🚀 快速測試

### 啟動伺服器
```bash
cd d:\Source\ADK
python api_server.py
```

### 查看所有可用 Agent
```bash
curl http://localhost:8000/agents
```

### 回應範例
```json
{
  "agents": [
    {"agent_id": "meeting_room", "name": "會議室預約助理", "description": "協助查詢、預約和管理會議室"},
    {"agent_id": "assistant", "name": "智慧助理", "description": "查詢天氣和時間資訊"},
    {"agent_id": "it_helpdesk", "name": "IT 服務台", "description": "A2A 示範：協調網路、帳號、硬體、軟體等子 Agent"},
    {"agent_id": "expense", "name": "報銷審批", "description": "Sequential 工作流：提交 → 主管審核 → 總監審核 → 財務 → 付款"},
    {"agent_id": "research", "name": "研究助理", "description": "Parallel 工作流：同時搜尋新聞、論文、市場數據並彙整報告"},
    {"agent_id": "ticket", "name": "客服工單", "description": "Loop 工作流：工單生命週期管理，狀態循環追蹤"},
    {"agent_id": "order", "name": "訂單處理", "description": "Human-in-the-Loop：自動化 + 人工審核的訂單處理流程"}
  ]
}
```

---

## 📁 專案結構

```
ADK/
├── api_server.py              # FastAPI 多 Agent 伺服器
├── API_DOCS.md               # API 文件
├── WORKFLOW_PATTERNS.md      # 本文件
├── meeting_room_agent/       # 會議室預約
│   ├── agent.py
│   ├── tools.py
│   └── __init__.py
├── my_agent/                 # 天氣/時間助理
│   └── ...
├── it_helpdesk_agent/        # A2A 示範
│   ├── agent.py
│   └── __init__.py
├── expense_agent/            # Sequential 示範
│   ├── agent.py
│   └── __init__.py
├── research_agent/           # Parallel 示範
│   ├── agent.py
│   └── __init__.py
├── ticket_agent/             # Loop 示範
│   ├── agent.py
│   └── __init__.py
└── order_agent/              # Human-in-the-Loop 示範
    ├── agent.py
    └── __init__.py
```

---

## 💡 設計原則

### 1. 單一職責
每個 Agent 專注於一個領域，工具函數也應該功能明確。

### 2. 清晰的狀態管理
使用明確的狀態機來管理工作流程，便於追蹤和除錯。

### 3. 錯誤處理
每個工具都應該回傳明確的狀態和錯誤訊息。

### 4. 可擴展性
新增 Agent 只需要：
1. 建立新的 agent 資料夾
2. 在 `api_server.py` 的 `load_agents()` 中註冊

### 5. 日誌記錄
完整的執行日誌有助於除錯和監控。
